# Database Configuration (Supabase Cloud PostgreSQL)
DATABASE_URL="postgresql://postgres.wuinvkwkiokmtecsttzj:[YOUR-PASSWORD]@aws-0-ap-northeast-1.pooler.supabase.com:5432/postgres?pgbouncer=true"
DIRECT_URL="postgresql://postgres.wuinvkwkiokmtecsttzj:[YOUR-PASSWORD]@aws-0-ap-northeast-1.pooler.supabase.com:5432/postgres"

# JWT Signing Secret — REQUIRED, the app will refuse to start without this.
# Generate a real value with: openssl rand -base64 48
# Never reuse the same secret across environments (dev/staging/prod), and
# never commit the real value to version control — only this placeholder
# belongs in .env.example. See backend/src/core/config/jwt-secret.ts for why.
JWT_SECRET="8KcxGpYrfgaGpv5MSC/He3LjFrRrpjghS7DDxm4fv1iVxYf1GcwrvCjVc53+0Rrq"

# Backend Server Configuration
PORT=4000
NODE_ENV=development

# Redis Configuration
REDIS_HOST="localhost"
REDIS_PORT=6379
